[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build && npm run railway:migrate"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "api"
